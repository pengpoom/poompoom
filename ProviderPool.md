# Provider 号池配置说明

更新时间：2026-05-30

本文档说明后台“接入 / Provider 号池”的配置方式。号池的目标是把不同上游账号按平台、套餐等级、充值等级、请求类型和质量要求分组调度，并在局部故障时自动回退。

## 1. 核心概念

### 池

池是一组同平台上游成员的集合，例如：

- `gpt-image 默认池`
- `gpt-image 高级池`
- `gemini-banana 默认池`

池只负责分组和匹配，不直接保存上游密钥。上游密钥保存在池成员里。

### 成员

成员是一个真实可请求的上游配置，包含：

- `Base URL`
- `API Key`
- 默认模型
- 优先级
- 权重
- 单成员并发上限
- 冷却时间
- 连续失败阈值
- 状态：`active` / `limited` / `unavailable`

实际生图请求最终会调度到某一个成员。

### fallback 池

fallback 池是默认兜底池。没有标签池命中，或者标签池命中但池内没有可用成员时，会尝试 fallback 池。

建议每个平台至少保留一个稳定的 fallback 池。

## 2. 匹配模式

池的匹配模式有三种：

| 模式 | 含义 | 适用场景 |
| --- | --- | --- |
| `fallback` | 默认兜底池，不靠标签命中 | 基础可用池、备用池 |
| `any` | 请求标签命中池标签中的任意一个即可 | 宽松匹配，如 `quality:high` 或 `tier:atelier` |
| `all` | 请求标签必须包含池配置的全部标签 | 精准匹配，如 `tier:meridian + quality:high` |

示例：

```text
匹配模式：all
调度标签：tier:meridian, quality:high
```

这表示只有最终请求标签同时包含 `tier:meridian` 和 `quality:high` 时，才会命中这个池。

```text
匹配模式：any
调度标签：tier:meridian, quality:high
```

这表示最终请求标签只要包含 `tier:meridian` 或 `quality:high` 中任意一个，就会命中这个池。

## 3. 标签来源

一次生图请求最终会合并两类标签。

### 请求标签

由生图请求本身产生：

```text
mode:generate
quality:high
size:1248x1248
model:gpt-image-2
```

后续如果生图工作区增加编辑、局部重绘、批量生成等模式，可以继续扩展 `mode:*` 标签。

### 用户标签

由当前用户身份和付费状态产生：

```text
role:user
tier:free
wallet:none
```

当前默认订阅等级：

| 展示名 | 标签 | 含义 |
| --- | --- | --- |
| Free | `tier:free` | 未订阅或默认等级 |
| Lumen | `tier:lumen` | 入门订阅 |
| Prism | `tier:prism` | 标准订阅 |
| Atelier | `tier:atelier` | 创作订阅 |
| Meridian | `tier:meridian` | 高阶订阅 |

当前默认充值等级：

| 展示名 | 标签 | 含义 |
| --- | --- | --- |
| None | `wallet:none` | 未充值 |
| Ember | `wallet:ember` | 小额充值 |
| Glow | `wallet:glow` | 中等充值 |
| Flare | `wallet:flare` | 高价值充值 |
| Radiant | `wallet:radiant` | 重度充值 |
| Zenith | `wallet:zenith` | 顶级充值用户 |

等级展示名和标签可以在后台“系统设置 / 计费等级”里调整。支付套餐可以绑定等级标签，用户完成对应套餐后，调度会自动带上对应标签。

## 4. 调度顺序

当前调度顺序如下：

1. 根据平台筛选启用池。
2. 用最终标签匹配 `any` / `all` 标签池。
3. 如果标签池命中，优先在命中的标签池里选择可用成员。
4. 如果标签池命中但没有可用成员，再尝试 fallback 池。
5. 如果没有标签池命中，直接尝试 fallback 池。
6. 如果号池没有可用成员，再尝试 API 接入或环境变量配置作为兜底。
7. 如果没有 API 兜底，Job 失败并记录失败原因。

成员选择会综合：

- 池是否启用
- 成员是否启用
- 成员状态是否不是 `unavailable`
- 冷却时间是否结束
- 单成员并发是否未满
- 池优先级
- 成员优先级
- 成员权重
- 最近使用时间

## 5. 推荐配置

### 最小可用配置

适合刚开始接入：

```text
池：gpt-image-default
平台：gpt-image
匹配模式：fallback
成员：至少 1 个 active 成员
```

这个池对所有订阅等级、所有充值等级、所有用户都可用。

### 高级订阅独立池

适合把高价值用户分到更稳定或更贵的上游：

```text
池：gpt-image-meridian
平台：gpt-image
匹配模式：all
调度标签：tier:meridian
```

只有 `tier:meridian` 用户会命中这个池。池内无可用成员时，会回退 fallback 池。

### 高质量请求独立池

适合高质量请求走更稳的上游：

```text
池：gpt-image-high-quality
平台：gpt-image
匹配模式：all
调度标签：quality:high
```

所有 `quality:high` 请求都会优先命中这个池。

### 高级订阅 + 高质量组合池

适合最精准的资源隔离：

```text
池：gpt-image-meridian-high
平台：gpt-image
匹配模式：all
调度标签：tier:meridian, quality:high
```

只有 Meridian 用户发起 High 质量请求时才会命中。

## 6. 两个同平台池的优先级

如果两个 `gpt-image` 标签池都能命中同一个请求，会按池优先级选择。数字越小越优先。

建议：

- 精准池优先级更小，例如 `10`
- 宽松池优先级稍大，例如 `30`
- fallback 池优先级可设为 `50`

示例：

| 池 | 模式 | 标签 | 优先级 |
| --- | --- | --- | --- |
| `gpt-image-meridian-high` | `all` | `tier:meridian, quality:high` | `10` |
| `gpt-image-high-quality` | `all` | `quality:high` | `30` |
| `gpt-image-default` | `fallback` | 空 | `50` |

当 Meridian 用户发起 High 请求时，会优先命中 `gpt-image-meridian-high`。

## 7. 成员策略

成员常用字段建议：

| 字段 | 建议 |
| --- | --- |
| 优先级 | 数字越小越优先；同一池内主账号可设 `10`，备用账号设 `30` |
| 权重 | 默认 `1`；想让某成员承担更多请求可提高 |
| 单成员并发 | 不确定上游承载能力时先设 `1` 或 `2` |
| 冷却时间 | 默认 `120` 秒；上游限流明显时提高 |
| 连续失败阈值 | 默认 `5`；失败达到阈值后自动降级为不可用 |

成员状态说明：

| 状态 | 含义 |
| --- | --- |
| `active` | 可参与调度 |
| `limited` | 暂时限流或冷却，调度会避开 |
| `unavailable` | 不可用，需要人工恢复 |

后台可以手动恢复成员为 `active`。

## 8. 预览调度

接入页的“预览调度”用于验证配置是否符合预期。它会模拟真实调度，但不会：

- 请求上游
- 占用成员并发
- 修改成员状态

建议每次调整池标签或套餐等级后，用预览检查：

- 最终标签是否符合预期
- 命中的池是否正确
- 命中的成员是否正确
- 是否发生“标签池回退默认池”
- 是否会走 API 兜底

## 9. 运营排障

运维页 Job 列表会展示：

- 来源：号池 / API 接入 / 环境变量
- 策略：标签池 / 标签池回退 / 兜底池 / API 兜底
- 池名
- 成员名
- 用户标签
- 最终标签

点击 Job 行可以打开详情抽屉，查看完整 provider、标签、耗时链路、错误和脱敏 payload。

常见问题：

### 为什么最高等级用户没有走高级池？

检查：

1. 用户当前订阅等级是否显示为预期等级。
2. 对应套餐是否绑定了正确 `tier:*` 标签。
3. 高级池是否启用。
4. 高级池平台是否和请求平台一致。
5. 高级池 `matchMode` 是否过严，例如 `all` 要求了额外标签。
6. 用“预览调度”确认最终标签里是否有目标标签。

### 为什么命中了标签池但最后走 fallback？

通常是标签池里没有可用成员。常见原因：

- 成员被禁用
- 成员状态是 `unavailable`
- 成员仍在冷却
- 单成员并发已满
- 成员平台和池平台不一致

运维页会显示策略为“标签池回退”。

### 为什么最后走 API 兜底？

说明号池没有可调度成员，系统尝试了 API 接入配置或环境变量配置。

如果不希望走 API 兜底，需要禁用或清空对应 API 接入配置。

### 为什么直接失败？

常见原因：

- 没有可用号池成员
- 没有 API 兜底配置
- 上游接口返回错误
- 上游返回 200 但没有图片数据

失败详情以 Job 错误码和错误消息为准。
